package org.mockito.mockito_all;

public interface Calculator {
//	
//	Employee obj = new Employee(10000); //make the value variable later
//										//gave an initial value to comply to Employee.java constructor

	
	public float computeTax(float amount);
	
	public double weeklySalary(Employee emp);
	
	public double fortnightSalary(double val); //replace the double by employee salary fortnight
	
	public double computeKiwiSaver(Employee emp);

	
}
